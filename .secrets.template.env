# ============================================================================
# SECRETS TEMPLATE FOR HETZNER DEPLOYMENT
# Copy this file to .secrets.env and fill in your values
# ============================================================================

# Hetzner Cloud API Token
# Get from: https://console.hetzner.cloud → Project → Security → API Tokens
HCLOUD_TOKEN=your-hetzner-cloud-api-token

# SSH Key (Public key content)
# Generate: ssh-keygen -t ed25519 -C "hetzner-k8s" -f ~/.ssh/hetzner_k8s
SSH_PUBLIC_KEY=ssh-ed25519 AAAA...

# Docker Hub Credentials
DOCKERHUB_USERNAME=your-dockerhub-username
DOCKERHUB_TOKEN=your-dockerhub-access-token

# Database Passwords (Generate strong passwords!)
# Use: openssl rand -base64 32
POSTGRES_AUTH_PASSWORD=strong-password-for-auth-db
POSTGRES_PRODUCT_PASSWORD=strong-password-for-product-db

# JWT Secret (Generate a strong secret!)
# Use: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key

# Let's Encrypt Email
LETSENCRYPT_EMAIL=ssl@yourcompany.com

# Domain Configuration
DOMAIN_NAME=yourcompany.com

# Slack Webhook (Optional - for notifications)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/xxx/yyy/zzz

# ============================================================================
# HOW TO USE
# ============================================================================
#
# 1. Copy this file:
#    cp .secrets.template.env .secrets.env
#
# 2. Fill in your values in .secrets.env
#
# 3. Load environment variables:
#    export $(cat .secrets.env | xargs)
#
# 4. For GitHub Actions, add these as repository secrets:
#    Settings → Secrets and variables → Actions → New repository secret
#
# ============================================================================
